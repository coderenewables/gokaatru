## Plan: Rewrite WORKFLOW_FRONTEND_SPEC.md to be a complete from-scratch build contract

The current spec was written when an existing frontend existed — it says "keep the API client", "reuse the store", "extend `refreshWorkspace`". That frontend is now deleted, so the spec is no longer self-sufficient. I'll rewrite it so it alone is enough to build the entire frontend from an empty `frontend/` directory.

### Edits to `D:\gokaatru\WORKFLOW_FRONTEND_SPEC.md` (no other files touched)

**1. Rewrite the document header (lines 1–5).**
Change "built on top of the existing React frontend" → "new frontend, `frontend/` does not currently exist, covers scaffolding from empty directory". Reframe audience to a from-scratch builder.

**2. Rewrite §0's closing paragraph (line 24).**
Remove "The app already has a Zustand store, React-Flow canvas, BYOK copilot, and compare view." Replace with the from-scratch framing: backend exposes a session HTTP API; the frontend is a SPA that creates a session, persists it, drives the 8 stages, and adds canvas/copilot/compare.

**3. Add §1.5 "Verified response shapes" (new section after §1.4).**
Enumerate the exact JSON fields for: `/health`, `/sessions/{id}` (SessionSummary), `/summary` (AnalysisSummary), `/sensors`, `/coverage/{sensor}`, `/statistics/{sensor}`, `/results/ltc`, `/results/ensemble`, `/clipping`, `/uncertainty`, `/map/site` GeoJSON, the 5 LTC algorithm path values, and the error model (400/404/502 + `X-GoKaatru-Session` header rule). These were confirmed against the backend routes.

**4. Replace §2 entirely ("Existing frontend — keep/change" → "Project scaffold from empty `frontend/`").**
- §2.1 Target directory layout tree (`frontend/` → `src/{types,lib,store,components,test}` with every file named and its owning phase).
- §2.2 `package.json` — full runtime + dev dependency lists, scripts.
- §2.3 Build configs — `vite.config.ts` (React plugin, `@` alias, vitest jsdom, `/api` dev proxy), `tsconfig.json` (strict, verbatimModuleSyntax, bundler resolution, paths), `vite-env.d.ts`.
- §2.4 API client contract — what `requestJson` must do (header auto-injection rule, error parsing, 204 handling) + the full exported wrapper list.
- §2.5 Brand palette.
- §2.6 Shared components list (moved here from old §2.4).

**5. Rewrite §3.2 ("Store additions" → "Build the store in full").**
Replace "Add to the existing store (keep all current fields/actions)" with the complete `WorkspaceStore` interface: all state fields (session/config/stage-derived/results/activity) and all actions (`bootstrapSession`, `restoreSession`, `refreshWorkspace`, `saveConfig`, `fetchPlot`, `refreshResults`, `invokeSessionOperation`, scenario + workflow actions). Add the localStorage key name and the `refreshWorkspace` → `computeStageStatuses` wiring.

**6. Add §3.3 "Stage-status logic"** — the `lib/stages.ts` contract: `STAGE_ORDER`, `STAGE_META` (each stage's `requiredSteps` against the verified `completed_steps` tokens), and the `computeStageStatuses` rule (done/locked/available).

**7. Expand §10 phases (A→G) into concrete file-level deliverables.**
Each phase becomes a checklist of files to create with a one-line scope and the gates (`npm run build` / `npm run test` / manual smoke). Phase A explicitly includes the scaffold, types, API client, config sync, store, stages lib, a minimal bootable `App.tsx`, and a vitest spec — enough to run end-to-end against the backend.

**8. Leave §4–§9 and §11 untouched** — those (stage specs, canvas/copilot/compare, type model, endpoint reference, definition of done) are already implementation-grade and backend-verified.

### What stays out of scope
No code files are created. Only the markdown spec is edited. Phase A implementation happens separately after the spec is approved.

### Verification before reporting done
Re-read the edited spec top-to-bottom and grep for residual "existing", "already", "reuse", "keep" phrasing in §0–§3 to confirm none of the old "extend the existing frontend" framing remains.