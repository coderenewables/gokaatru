FROM python:3.11-slim

ENV PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1

WORKDIR /app

COPY pyproject.toml ./
COPY server/ server/
COPY data/ data/

RUN pip install --no-cache-dir ".[ml]"

EXPOSE 8000 8080
